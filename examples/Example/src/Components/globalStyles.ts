import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  fill: {
    width: '100%',
    height: '100%',
  },
  pressed: {
    opacity: 0.6,
  },
  centerContents: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
